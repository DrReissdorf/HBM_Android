package home.sven.hbm_android;

public class SharedPrefDefaults {
    public final static int DEFAULT_ACTIVATION_LIMIT = 1500; //standard value if no sharedpref is found
    public final static int DEFAULT_DEACTIVATION_LIMIT = 750; //standard value if no sharedpref is found
    public final static int DEFAULT_ACTIVATION_FULLSLEEP = 1500; //standard value if no sharedpref is found
    public final static int DEFAULT_DEACTIVATION_FULLSLEEP = 3000; //standard value if no sharedpref is found
    public final static int DEFAULT_AVERAGE_VALUES = 10; //standard value if no sharedpref is found
}
